extends layout 

block content
    h2 CHATBOT NAME : 
        p#port #{port}
        p#bot_id #{bot_id}
    div.messaging-history 
        ul#messages-list
            
    div.message-bar
        input#message-input(type="text", placeholder="Type your message here...", value='')
        input#send-button(type="button", value="Send")

    script(src="/scripts/messenger.js")